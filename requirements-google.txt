# Runtime dependencies for the All Things Agentic Hackathon submission.
# Google Gen AI SDK is the required Google agent framework. The deployed service
# invokes Gemini through Vertex AI using Application Default Credentials.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.8,<3.0
aiosqlite>=0.20,<1.0
google-genai>=1.0,<2.0
google-auth>=2.35,<3.0

# Test-only dependency (not required by the Cloud Run container).
pytest>=8.0,<9.0
